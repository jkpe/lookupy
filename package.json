{
  "name": "lookupy",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "node scripts/copy-www.mjs",
    "cap:sync": "npm run build:web && npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0",
    "typescript": "^5.9.3"
  }
}
